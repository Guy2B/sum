import {createGovernanceOrchestrator} from '../modules/governance-v2/governance-orchestrator.mjs';import {createPolicy} from '../modules/governance-v2/policy-contract.mjs';import {createIdentityContext} from '../modules/governance-v2/identity-context.mjs';const governance=createGovernanceOrchestrator({policies:[createPolicy({id:'allow-owner',name:'Owner access',effect:'allow',resource:'signal',actions:['read'],conditions:[{path:'identity.roles',operator:'contains',value:'owner'}]})]});function render(decision=null){const protectedData=governance.protect({title:'Facture',email:'finance@example.com',token:'secret-value'});document.querySelector('#summary').innerHTML=[['Autorisation',decision?.allowed?'autorisée':'en attente'],['Classification',protectedData.classification.classification],['Audit',governance.audit().length]].map(([k,v])=>`<article class="card"><span>${k}</span><h2>${v}</h2></article>`).join('');document.querySelector('#details').innerHTML=`<article class="card"><strong>Données protégées</strong><pre>${JSON.stringify(protectedData.record,null,2)}</pre></article>`}document.querySelector('#evaluate').onclick=()=>render(governance.authorize({identity:createIdentityContext({subjectId:'user-1',roles:['owner']}),resource:'signal',action:'read'}));render();