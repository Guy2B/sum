import {createAgentRuntime} from '../modules/agents/agent-runtime.mjs';import {createAgent} from '../modules/agents/agent-contract.mjs';import {createTaskEnvelope} from '../modules/agents/task-envelope.mjs';const agents=[createAgent({id:'planner',name:'Planner',capabilities:['planning','calendar']})];const runtime=createAgentRuntime({agents,policy:{allowedTools:['calendar'],blockedActions:['delete'],approvalThreshold:.7}});function run(){const task=createTaskEnvelope({id:'task_1',goal:'Préparer la semaine',requiredCapabilities:['planning'],input:{steps:[{action:'read-calendar',risk:.2},{action:'propose-plan',risk:.4},{action:'write-calendar',risk:.8}]}});const prepared=runtime.prepare(task);const rows=prepared.steps.map(step=>({step,decision:runtime.authorize(step,'calendar')}));document.querySelector('#summary').innerHTML=[['Agent',prepared.agent.name],['Étapes',rows.length],['Approbations',rows.filter(x=>x.decision.approvalRequired).length]].map(([l,v])=>`<article class="card"><span>${l}</span><h2>${v}</h2></article>`).join('');document.querySelector('#steps').innerHTML=rows.map(x=>`<article class="card"><strong>${x.step.sequence}. ${x.step.action}</strong><p>${x.decision.allowed?'Autorisé':'Bloqué'} · ${x.decision.approvalRequired?'Approbation requise':'Sans approbation'}</p></article>`).join('')}document.querySelector('#run').onclick=run;run();