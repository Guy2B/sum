import {createProductionPlatform} from '../modules/platform/production-platform.mjs';import {createDeploymentUnit} from '../modules/platform/deployment-unit.mjs';const platform=createProductionPlatform();platform.register({id:'api',name:'Sigma API',status:'healthy'});platform.register({id:'worker',name:'Sigma Worker',status:'healthy'});let version=1;function render(plan=[]){const status=platform.status();document.querySelector('#summary').innerHTML=[['Services',status.services],['Sains',status.healthyServices],['Déploiements',status.deployments],['Incidents',status.activeIncidents]].map(([l,v])=>`<article class="card"><span>${l}</span><h2>${v}</h2></article>`).join('');document.querySelector('#deployments').innerHTML=plan.map(batch=>`<article class="card"><strong>Lot ${batch.sequence}</strong><p>${batch.units.map(x=>`${x.id}@${x.version}`).join(', ')}</p><small>${batch.strategy}</small></article>`).join('')}document.querySelector('#deploy').onclick=()=>{version++;const units=[createDeploymentUnit({id:'api',version:`1.${version}.0`,artifact:'sigma-api.zip',environment:'production'}),createDeploymentUnit({id:'worker',version:`1.${version}.0`,artifact:'sigma-worker.zip',environment:'production'})];render(platform.deploy(units,{strategy:'rolling',batchSize:1}))};render();