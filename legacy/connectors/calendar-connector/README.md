# Σ Calendar Connector

Read-only OAuth connector for Google Calendar and Microsoft Outlook/Microsoft 365 calendars.

```bash
cp .env.example .env
npm install
npm run check
npm start
```

The connector imports events into the local Σ planner. It does not create, modify or delete remote events in V1.7.
