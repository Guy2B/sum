'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {createApprovalAgent}=require('../../modules/agents/approval-agent');
test('Sprint 14 external writes remain approval gated',async()=>{const agent=createApprovalAgent({executor:async action=>({sent:action.payload})});const proposal=await agent.propose({id:'p1',kind:'external_write',confidence:.9,scope:['mail'],payload:'hello'});assert.equal(proposal.status,'awaiting_approval');const result=await agent.approve('p1','u1');assert.equal(result.status,'executed');assert.equal(agent.pending().length,0);});
