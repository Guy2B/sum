import test from 'node:test';import assert from 'node:assert/strict';import {buildDashboard} from '../../modules/dashboard/dashboard-projection.js';test('Sprint 42 produces visible dashboard counters and priorities',()=>{const d=buildDashboard({goals:[{status:'active'}],tasks:[{id:'t',status:'open',priority:90}],now:new Date('2026-01-01')});assert.equal(d.counters.activeGoals,1);assert.equal(d.priorities[0].id,'t');});