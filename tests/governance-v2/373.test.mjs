import test from 'node:test';import assert from 'node:assert/strict';import {createGovernanceOrchestrator} from '../../modules/governance-v2/governance-orchestrator.mjs';test('Sprint 373',()=>{const g=createGovernanceOrchestrator();g.addPolicy({id:'p',effect:'allow',resource:'x',actions:['read'],conditions:[]});assert.equal(g.authorize({identity:{subjectId:'u'},resource:'x',action:'read'}).allowed,true);});
