'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {createPersistentGraph,createMemoryStore}=require('../../modules/knowledge/persistent-graph');
test('Sprint 8 persists nodes and resolves neighbors',async()=>{const graph=createPersistentGraph({store:createMemoryStore()});await graph.upsertNode({type:'person',id:'u1',name:'Guy'});await graph.upsertNode({type:'project',id:'p1',name:'Sigma'});await graph.link({from:'person:u1',to:'project:p1',type:'owns'});assert.equal((await graph.neighbors('person:u1'))[0].name,'Sigma');assert.equal((await graph.snapshot()).edges.length,1);});
