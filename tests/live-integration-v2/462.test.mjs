import test from 'node:test';import assert from 'node:assert/strict';import {renderLiveIntegration} from '../../modules/live-integration-v2/dom-integration.mjs';test('Sprint 462',()=>{const nodes=new Map([['[data-live-connected]',{textContent:''}],['[data-live-priorities]',{textContent:''}],['[data-live-unread]',{textContent:''}],['[data-live-awaiting-reply]',{textContent:''}],['[data-live-total]',{textContent:''}]]);const root={querySelector:s=>nodes.get(s)||null};renderLiveIntegration(root,{counters:{connected:1,priorities:2,unread:3,awaitingReply:0,total:4},sync:{}});assert.equal(nodes.get('[data-live-total]').textContent,'4');});
