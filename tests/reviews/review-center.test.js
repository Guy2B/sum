import test from 'node:test';import assert from 'node:assert/strict';import {createReview} from '../../modules/reviews/review-center.js';test('Sprint 46 generates a concrete review',()=>{const r=createReview({workspaceId:'w',period:'week',goals:[{id:'g',status:'active',progress:10}],tasks:[{title:'Done',status:'done'}]});assert.equal(r.metrics.completedTasks,1);assert.equal(r.risks[0].goalId,'g');});