import test from 'node:test';import assert from 'node:assert/strict';import {createIntegrationOrchestrator} from '../../modules/integrations/integration-orchestrator.mjs';test('Sprint 252',async ()=>{const o=createIntegrationOrchestrator({provider:'p',accountId:'a',kind:'email',fetchPage:async()=>({items:[{id:'1'}],nextCursor:null})});const r=await o.sync();assert.equal(r.batch.count,1);});
