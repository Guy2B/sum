'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {createProfileRepository,createProfileService}=require('../../modules/identity/profile-service');
test('Sprint 6 creates and versions identity profiles',async()=>{const service=createProfileService({repository:createProfileRepository()});const created=await service.create({id:'u1',displayName:'Guy',email:'GUY@EXAMPLE.COM',now:'2026-01-01T00:00:00.000Z'});assert.equal(created.email,'guy@example.com');const updated=await service.update('u1',{displayName:'Guy B',now:'2026-01-02T00:00:00.000Z'});assert.equal(updated.version,2);assert.equal(updated.displayName,'Guy B');});
