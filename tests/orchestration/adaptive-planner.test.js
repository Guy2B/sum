'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {buildAdaptivePlan,replan}=require('../../modules/orchestration/adaptive-planner');
test('Sprint 10 planner respects capacity and replans outcomes',()=>{const items=[{id:'a',priority:5,impact:2,effort:3},{id:'b',priority:2,effort:2},{id:'c',priority:1,effort:4}];const plan=buildAdaptivePlan(items,{capacity:5,now:0});assert.deepEqual(plan.selected.map(x=>x.id),['a','b']);const next=replan(plan,[{id:'a',status:'done'}],{capacity:4,now:0});assert.equal(next.selected.some(x=>x.id==='a'),false);});
