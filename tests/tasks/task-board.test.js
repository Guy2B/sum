import test from 'node:test';import assert from 'node:assert/strict';import {ApplicationStore} from '../../modules/application/application-store.js';import {ApplicationApi} from '../../modules/api/application-api.js';import {TaskBoard} from '../../modules/tasks/task-board.js';test('Sprint 44 moves tasks across a usable board',()=>{const actor={workspaceId:'w',userId:'u'};const b=new TaskBoard({api:new ApplicationApi({store:new ApplicationStore()}),actor});b.create({id:'t',title:'Task'});b.move('t','done');assert.equal(b.columns().done.length,1);});