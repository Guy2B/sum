import test from 'node:test';import assert from 'node:assert/strict';import {ApplicationStore} from '../../modules/application/application-store.js';import {InteractiveDailyPlan} from '../../modules/planning/interactive-daily-plan.js';test('Sprint 45 lets users accept and confirm daily plans',()=>{const p=new InteractiveDailyPlan({store:new ApplicationStore()});const plan=p.create({workspaceId:'w',date:'2026-01-01',items:[{id:'i'}]});p.decide(plan.id,'i','accepted');assert.equal(p.confirm(plan.id).status,'confirmed');});