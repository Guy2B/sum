import test from 'node:test';import assert from 'node:assert/strict';import {createExecutionOrchestrator} from '../../modules/execution-v2/execution-orchestrator.mjs';test('Sprint 433',async ()=>{const o=createExecutionOrchestrator({capabilities:{execute:async()=>({ok:true})},policy:{allowedCapabilities:['x']}});const r=await o.execute({id:'i',actorId:'u',actions:[{id:'a',capability:'x',risk:'low'}]});assert.equal(r.status,'completed');});
