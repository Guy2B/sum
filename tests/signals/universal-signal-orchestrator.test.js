import test from'node:test';import assert from'node:assert/strict';import{processSignals}from'../../modules/signals/universal-signal-orchestrator.js';test('processes signals end to end',()=>{const [r]=processSignals([{source:'email',subject:'Recruteur : entretien demain',text:'Confirmer rapidement'}],['jobSeeker']);assert.equal(r.classification.domain,'employment');assert.ok(r.action.title);});
