'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {assessReadiness}=require('../../modules/release/readiness');
test('Sprint 15 readiness blocks missing release gates',()=>{const blocked=assessReadiness({testsPassed:true,repositoryAuditPassed:true,exposedSecrets:0,coverage:60,minimumCoverage:70,migrationsValidated:true});assert.equal(blocked.ready,false);assert.deepEqual(blocked.failures,['coverage']);const ready=assessReadiness({testsPassed:true,repositoryAuditPassed:true,exposedSecrets:0,coverage:80,migrationsValidated:true});assert.equal(ready.grade,'RC');});
