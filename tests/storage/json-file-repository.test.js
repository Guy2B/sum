import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp,rm} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {JsonFileRepository} from '../../modules/storage/json-file-repository.js';test('Sprint 47 persists and reloads product data',async()=>{const d=await mkdtemp(join(tmpdir(),'sigma-'));const r=new JsonFileRepository(join(d,'data.json'));await r.save({goals:[{id:'g'}]});assert.equal((await r.load()).goals[0].id,'g');await rm(d,{recursive:true,force:true});});