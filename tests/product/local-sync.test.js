import test from 'node:test';import assert from 'node:assert/strict';import {serializeState,deserializeState,mergeState} from '../../modules/product/local-sync.js';test('Sprint 57 persists and merges local state',()=>{const state={goals:[{id:'g'}],tasks:[],reviews:[],plan:[]};assert.equal(deserializeState(serializeState(state)).goals[0].id,'g');assert.equal(mergeState(state,{goals:[{id:'g'}],tasks:[],reviews:[]}).goals.length,1);});