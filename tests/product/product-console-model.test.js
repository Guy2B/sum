import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {ApplicationStore} from '../../modules/application/application-store.js';import {ApplicationApi} from '../../modules/api/application-api.js';import {createConsoleModel} from '../../modules/product/product-console-model.js';test('Sprint 48 ships a visible console and interactive model',async()=>{const actor={workspaceId:'w',userId:'u'};const m=createConsoleModel({api:new ApplicationApi({store:new ApplicationStore()}),actor});assert.equal(m.addGoal('Goal').counters.activeGoals,1);const html=await readFile(new URL('../../product/sigma-console.html',import.meta.url),'utf8');assert.match(html,/Tableau de bord produit|Bonjour, voici votre journée\./);});
