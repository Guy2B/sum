import test from 'node:test';import assert from 'node:assert/strict';import {createLearningOrchestrator} from '../../modules/learning-v2/learning-orchestrator.mjs';test('Sprint 388',()=>{const o=createLearningOrchestrator({policy:{allowedSources:['ui'],requireSubject:true}});const r=o.ingest({id:'e',type:'feedback',source:'ui',subjectId:'u',payload:{rating:.8}});assert.equal(r.accepted,true);});
