import test from 'node:test';import assert from 'node:assert/strict';import {createPlanningOrchestrator} from '../../modules/planning-v2/planning-orchestrator.mjs';test('Sprint 418',()=>{const o=createPlanningOrchestrator();o.registerGoal({id:'g'});o.registerTasks('g',[{id:'t',urgency:1,hours:1,status:'pending'}]);assert.equal(o.buildPlan('g',{capacityHours:1}).selected.length,1);});
