'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {createAuditLog}=require('../../modules/observability/audit-log');const {evaluateAction}=require('../../modules/security/policy-engine');
test('Sprint 11 audit chain verifies and policy blocks unsafe action',()=>{const log=createAuditLog();log.append({type:'one',occurredAt:'2026-01-01T00:00:00.000Z'});log.append({type:'two',occurredAt:'2026-01-02T00:00:00.000Z'});assert.equal(log.verify(),true);assert.equal(evaluateAction({kind:'external_write',confidence:.9,scope:['mail']}).allowed,false);});
