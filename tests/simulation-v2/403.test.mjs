import test from 'node:test';import assert from 'node:assert/strict';import {createSimulationOrchestrator} from '../../modules/simulation-v2/simulation-orchestrator.mjs';test('Sprint 403',()=>{const o=createSimulationOrchestrator();const r=o.evaluate({scenario:{id:'s',objectives:[{metric:'x',target:1}]},options:[{id:'a'}],simulate:()=>({x:1})});assert.equal(r.recommendation.id,'a');});
