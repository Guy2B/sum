'use strict';
function createConnectorRegistry(){const connectors=new Map();return Object.freeze({register(connector){if(!connector?.id||typeof connector.pull!=='function')throw new TypeError('connector id and pull are required');if(connectors.has(connector.id))throw new Error('connector_exists');connectors.set(connector.id,Object.freeze({...connector,capabilities:Object.freeze([...(connector.capabilities||[])])}));return connector.id;},get(id){return connectors.get(id)||null;},list(capability){return [...connectors.values()].filter(c=>!capability||c.capabilities.includes(capability));},async pullAll(context={}){const results=[];for(const connector of connectors.values()){try{results.push({id:connector.id,status:'fulfilled',value:await connector.pull(context)});}catch(error){results.push({id:connector.id,status:'rejected',error:error.message});}}return results;}});}
module.exports={createConnectorRegistry};
