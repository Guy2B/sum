export function inferDeadline(s,now=new Date()){if(s.deadline)return{deadline:s.deadline,source:'explicit',confidence:1};const t=`${s.title||''} ${s.body||''}`.toLowerCase();let m=t.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{4})\b/);if(m)return{deadline:`${m[3]}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}T23:59:59`,source:'text-date',confidence:.9};const d=new Date(now);if(/demain/.test(t)){d.setDate(d.getDate()+1);d.setHours(23,59,59,0);return{deadline:d.toISOString(),source:'relative-date',confidence:.82}}if(/aujourd['’]hui/.test(t)){d.setHours(23,59,59,0);return{deadline:d.toISOString(),source:'relative-date',confidence:.86}}return{deadline:null,source:'none',confidence:0}}
