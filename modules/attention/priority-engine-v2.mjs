const C=['mise en demeure','amende','contravention','prélèvement rejeté','urgent','convocation'];const H=['facture','relance','échéance','école','recruteur','offre d’emploi','offre d\'emploi','cpam','caf','urssaf','impôt','assurance','autorisation','impayé'];const B={family:['école','crèche','cantine','enfant'], 'job-seeker':['recruteur','entretien','candidature','offre','france travail'],creator:['brief','publication','partenariat','livrable'],freelancer:['client','devis','facture','tva','urssaf','impayé'],'small-business':['trésorerie','fournisseur','paie','tva'],frontier:['frontalier','permis','fiscal','assurance maladie','change'],student:['examen','inscription','université','bourse'],caregiver:['aidant','traitement','ordonnance','soins'],personal:['banque','santé','administratif','facture']};export function scorePriority(s,c={}){const t=`${s.title||''} ${s.body||''}`.toLowerCase();let u=10,i=10,r=10,why=[];if(C.some(x=>t.includes(x))){u+=45;r+=30;why.push('Indicateur critique détecté.')}else if(H.some(x=>t.includes(x))){u+=25;i+=25;why.push('Signal important détecté.')}for(const e of(c.editions?.length?c.editions:['personal']))if((B[e]||[]).some(x=>t.includes(x))){i+=25;why.push(`Pertinent pour l’édition ${e}.`)}if(s.deadline){const h=(new Date(s.deadline)-(c.now?new Date(c.now):new Date()))/36e5;if(h<=0){u+=45;r+=35;why.push('Échéance dépassée.')}else if(h<=24){u+=40;r+=25;why.push('Échéance dans moins de 24 heures.')}else if(h<=72){u+=25;r+=15;why.push('Échéance proche.')}else if(h<=168){u+=12;why.push('Échéance cette semaine.')}}const score=Math.min(100,Math.round(u*.45+i*.35+r*.2));return{score,level:score>=75?'critical':score>=55?'high':score>=35?'today':score>=20?'week':'info',confidence:Math.min(.98,.55+why.length*.09),dimensions:{urgency:Math.min(100,u),impact:Math.min(100,i),risk:Math.min(100,r)},reasons:why.length?why:['Aucun facteur majeur détecté.']}}
