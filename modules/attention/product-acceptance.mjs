export function validateAttentionCenter({queue,dashboard,approvals=[]}={}){const f=[];for(const l of['critical','high','today','week','info'])if(!Array.isArray(queue?.groups?.[l]))f.push(`missing group: ${l}`);if(!dashboard?.counters?.length)f.push('dashboard counters missing');if(Object.values(queue?.groups||{}).flat().some(x=>!x.explanation?.summary))f.push('unexplained priorities');if(approvals.some(a=>a.sensitive&&a.status!=='pending-approval'))f.push('sensitive action bypassed approval');return{ok:!f.length,failures:f}}
