export function explainPriority(s,a){return{headline:`${a.level} · score ${a.score}/100`,summary:a.reasons.join(' '),evidence:[['Source',s.source||'inconnue'],['Échéance',s.deadline||'non détectée'],['Confiance',`${Math.round((a.confidence||0)*100)} %`],['Urgence',a.dimensions?.urgency??0],['Impact',a.dimensions?.impact??0],['Risque',a.dimensions?.risk??0]].map(([label,value])=>({label,value})),reversible:true}}
