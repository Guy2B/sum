export function canStoreMemory(m,consent={}){const s=consent[m.owner]||consent.default||{};if(s.enabled===false)return{allowed:false,reason:'memory-disabled'};if(s.types&&!s.types.includes(m.type))return{allowed:false,reason:'type-not-consented'};if(s.sources&&!s.sources.includes(m.source))return{allowed:false,reason:'source-not-consented'};return{allowed:true,reason:'consented'}}
