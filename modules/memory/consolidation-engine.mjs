export function consolidateMemories(ms=[],{groupBy=m=>`${m.type}:${m.owner}`}={}){const g=new Map();for(const m of ms){const a=g.get(groupBy(m))||[];a.push(m);g.set(groupBy(m),a)}return[...g.entries()].map(([key,v])=>({key,count:v.length,memoryIds:v.map(x=>x.id),averageImportance:v.reduce((s,x)=>s+x.importance,0)/v.length,averageConfidence:v.reduce((s,x)=>s+x.confidence,0)/v.length,summary:v.map(x=>String(x.content)).join(' | ')}))}
