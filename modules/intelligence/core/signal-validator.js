'use strict';
(function(root,factory){const api=factory(typeof module==='object'&&module.exports?require('./signal-schema.js'):root.SigmaIntelligenceV1.SignalSchema);if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.SigmaIntelligenceV1=root.SigmaIntelligenceV1||{};root.SigmaIntelligenceV1.SignalValidator=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(Schema){
  function validate(signal){const errors=[];if(!signal||typeof signal!=='object')return{valid:false,errors:['signal must be an object']};if(signal.schemaVersion!==Schema.SCHEMA_VERSION)errors.push('unsupported schemaVersion');if(!Schema.DOMAINS.includes(signal.source?.domain))errors.push('source.domain is invalid');if(!signal.source?.sourceId)errors.push('source.sourceId is required');if(!signal.source?.provider)errors.push('source.provider is required');if(!signal.content?.title&&!signal.content?.body)errors.push('content.title or content.body is required');if(!signal.content?.receivedAt||!Number.isFinite(new Date(signal.content.receivedAt).getTime()))errors.push('content.receivedAt is invalid');if(!signal.relationships?.deduplicationKey)errors.push('relationships.deduplicationKey is required');return{valid:errors.length===0,errors};}
  function assert(signal){const result=validate(signal);if(!result.valid)throw new TypeError(`Invalid SigmaSignal: ${result.errors.join(', ')}`);return signal;}
  return Object.freeze({validate,assert});
});
