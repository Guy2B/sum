'use strict';
(function(root,factory){const api=factory(typeof module==='object'&&module.exports?require('./_shared.js'):root.SigmaIntelligenceV1.AdapterShared);if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.SigmaIntelligenceV1=root.SigmaIntelligenceV1||{};root.SigmaIntelligenceV1.EventSignalAdapter=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(S){return Object.freeze({normalize(item){const received=item.startAt||item.start||((item.date||'')+(item.time?`T${item.time}:00`:'T00:00:00'));const signal=S.signal('calendar',item,{provider:item.provider||'calendar',sourceType:'event',receivedAt:received,title:item.title,body:item.description||item.location});if(signal&&item.date)signal.entities.dates.push({value:item.date,type:'explicit',role:'start',confidence:1});return signal;},normalizeMany(rows=[]){return rows.map(this.normalize).filter(Boolean);}});});
