'use strict';
(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.SigmaIntelligenceV1=root.SigmaIntelligenceV1||{};root.SigmaIntelligenceV1.ClassificationService=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(){
 const RULES={opportunity:['devis','offre','proposal','quote','pricing','client','prospect','candidature','job','poste'],administration:['facture','invoice','payment','paiement','contrat','subscription','abonnement','document'],risk:['urgent','incident','blocked','bloqué','erreur','failure','échec','security','sécurité'],content:['contenu','content','publication','post idea','idée de post']};
 function classify(signal){const domain=signal.source.domain,text=`${signal.content.title} ${signal.content.summary} ${signal.content.body}`.toLowerCase();if(domain==='task')return result('execution',.99,['domain:task']);if(domain==='calendar')return result('calendar',.99,['domain:calendar']);if(domain==='opportunity')return result('opportunity',.99,['domain:opportunity']);for(const [category,words] of Object.entries(RULES)){const matches=words.filter(w=>text.includes(w));if(matches.length)return result(category,Math.min(.95,.68+matches.length*.08),matches.map(w=>`keyword:${w}`));}return result(domain==='social'?'relationship':'communication',.72,[`domain:${domain}`]);}
 function result(category,confidence,matchedRules){return{category,subcategory:'',confidence:{score:Math.round(confidence*100),reasons:matchedRules},matchedRules};}
 return Object.freeze({version:'1.0.0-rules',classify});
});
