'use strict';
(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.SigmaIntelligenceV1=root.SigmaIntelligenceV1||{};root.SigmaIntelligenceV1.RecommendationService=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(){
 function recommend(signal,intel){let action='read',label='Lire',requiresApproval=false;if(intel.replyExpected){action='reply';label='Préparer une réponse';requiresApproval=true;}else if(signal.source.domain==='calendar'){action='prepare_meeting';label='Préparer le rendez-vous';}else if(intel.category==='opportunity'){action='create_opportunity';label='Créer une opportunité';requiresApproval=true;}else if(signal.source.domain==='task'){action=intel.urgency.dueAt?'create_reminder':'read';label=intel.urgency.dueAt?'Créer un rappel':'Examiner la tâche';requiresApproval=action!=='read';}else if(intel.category==='content'){action='create_content_idea';label='Créer une idée de contenu';requiresApproval=true;}const rationaleCodes=[...intel.importance.reasons,...intel.urgency.reasons,...intel.impact.reasons,`category:${intel.category}`];return{action,label,rationaleCodes,dueAt:intel.urgency.dueAt||null,requiresApproval,sensitivity:requiresApproval?'controlled':'low',alternatives:['read','ignore','archive'].filter(a=>a!==action)};}
 return Object.freeze({recommend});
});
