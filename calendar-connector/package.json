{
  "name": "sigma-calendar-connector",
  "version": "1.7.0",
  "private": true,
  "description": "Read-only Google and Microsoft calendar connector for Sigma Life OS",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/state.js && node --check lib/providers/google.js && node --check lib/providers/microsoft.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "googleapis": "^156.0.0",
    "helmet": "^8.1.0"
  }
}
